# 📦 glTF（ジーエルティーエフ）入門

> **この章で学ぶこと:** VRMの土台となっている「glTF」という3Dファイル形式について理解する

---

## 🎯 glTFとは？

glTFは、3Dモデルを表示するために作られた、現在もっとも主流な**ファイル形式**（データの保存形式）の1つです。よく **「3D界のJPEG」** と例えられます 🖼️。

画像におけるJPEGのように、**ファイルサイズが小さく、Webブラウザやアプリ上ですぐに読み込んで表示できる** ように設計されているのが特徴です。

---

## 🤔 なぜglTFが重要なの？

### VRMとの深い関係

**VRMはglTFの「拡張版」** です！

```
glTF（標準3D形式）
  └── VRM（glTF + アバター専用データ）
        ├── 表情データ
        ├── 視線制御
        ├── 揺れもの設定
        └── ライセンス情報など
```

このため、コード中に出てくる `GLTFLoader`（glTFローダー）を使ってVRMを読み込むのは、VRMがglTFの仕組みを利用しているからなんです。

### 他の形式との比較

| 形式 | 特徴 | 用途 |
|-----|------|-----|
| **.obj** | 古い形式、テクスチャ別ファイル | 静止モデル |
| **.fbx** | 多機能だが独自形式 | ゲーム開発 |
| **.glTF/.glb** ⭐ | 軽量・標準規格・Web最適 | Web3D |
| **.vrm** | glTF + アバター情報 | 3Dアバター |

---

## 📁 glTFファイルの中身

glTFファイルには以下のようなデータが含まれています：

```
glTFファイル
├── 🔺 メッシュ（形）      - 3Dの形状データ
├── 🎨 マテリアル（素材）  - 色や質感の情報
├── 🖼️ テクスチャ（画像）  - 貼り付ける画像
├── 🦴 ボーン（骨格）      - アニメーション用の骨
├── 🎬 アニメーション      - 動きのデータ
└── 📐 シーン構造         - オブジェクトの配置
```

### ファイル形式の種類

| 拡張子 | 内容 |
|-------|------|
| `.gltf` | JSON形式（テキスト）+ 外部ファイル参照 |
| `.glb` | バイナリ形式（1ファイルに全部入り）|
| `.vrm` | glbベース + VRM専用データ |

---

## 💻 Three.jsでの読み込み方

```javascript
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';

const loader = new GLTFLoader();

// 基本的な読み込み
loader.load('model.glb', (gltf) => {
  scene.add(gltf.scene);  // シーンに追加
});
```

VRM 1.0では、このGLTFLoaderに**プラグイン**を追加して使います：

```javascript
import { VRMLoaderPlugin } from '@pixiv/three-vrm';

loader.register((parser) => new VRMLoaderPlugin(parser));

loader.load('character.vrm', (gltf) => {
  const vrm = gltf.userData.vrm;  // VRMデータはここに！
  scene.add(vrm.scene);
});
```

---

## 📖 次のステップ

glTFの基本が分かったら、実際にVRMモデルを動かしてみましょう！

👉 [example-001](../example-001/README.MD) でアニメーション再生に挑戦！

---

## 🔗 参考リンク

- [glTF公式仕様](https://www.khronos.org/gltf/)
- [Three.js GLTFLoader ドキュメント](https://threejs.org/docs/#examples/en/loaders/GLTFLoader)
- [VRM公式サイト](https://vrm.dev/)